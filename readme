pip install -r requirements.txt


use this to install all the libraries required to run this script



but before that make the environment variable for it

run this

python3 -m venv .venv
source .venv/bin/activate


python3 DynamicHandler.py --url 'https://fa-etan-dev14-saasfademo1.ds-fa.oraclepdemos.com' --username 'Casey.Brown' --password 'Ne2?4eW*'
